#include <bits/stdc++.h>
using namespace std;

int search(vector<int> nums, int target)
{
    int mid = nums.size()/2;
    
}

int main()
{
    int n;
    int target;
    vector<int> nums(n, 0);
    for(int i = 0; i < n; i++)
    {
        cin >> nums[i];   
    }
    return 0;
}